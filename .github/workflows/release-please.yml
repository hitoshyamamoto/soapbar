name: Release Please

on:
  push:
    branches: [main]

permissions: {}

concurrency:
  group: release-please
  cancel-in-progress: false

jobs:
  release-please:
    name: Maintain release PR / cut release
    runs-on: ubuntu-latest
    permissions:
      contents: write       # push the release PR branch, create tag + GitHub Release
      pull-requests: write  # open/update the release PR
      actions: write        # dispatch release.yml after a release is cut
    steps:
      # Release PRs created with the default GITHUB_TOKEN get CI runs stuck in
      # action_required (manual "Approve and run"). A GitHub App token avoids
      # that. Optional: when the app is not configured (variable unset), the
      # step is skipped and everything falls back to GITHUB_TOKEN.
      - name: Mint GitHub App token
        id: app-token
        if: ${{ vars.RELEASE_BOT_APP_ID != '' }}
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1  # v3.2.0
        with:
          app-id: ${{ vars.RELEASE_BOT_APP_ID }}
          private-key: ${{ secrets.RELEASE_BOT_PRIVATE_KEY }}

      - uses: googleapis/release-please-action@45996ed1f6d02564a971a2fa1b5860e934307cf7  # v5.0.0
        id: release
        with:
          token: ${{ steps.app-token.outputs.token || github.token }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

      # release-please bumps pyproject.toml but not the project's own version
      # recorded in uv.lock; regenerate the lockfile on the release PR branch
      # so the release commit is internally consistent.
      - name: Check out release PR branch
        if: ${{ steps.release.outputs.prs_created == 'true' }}
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          # `|| '{}'`: expressions in `with`/`env` are evaluated even for
          # skipped steps, so guard against the output being empty.
          ref: ${{ fromJSON(steps.release.outputs.pr || '{}').headBranchName }}
          # Push the uv.lock sync with the app token too: pushes made with
          # GITHUB_TOKEN don't trigger the PR's CI (anti-recursion), which
          # would leave the release PR's head commit without required checks.
          token: ${{ steps.app-token.outputs.token || github.token }}

      - name: Install uv
        if: ${{ steps.release.outputs.prs_created == 'true' }}
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9  # v9.0.0
        with:
          version: "latest"

      - name: Sync uv.lock with the bumped version
        if: ${{ steps.release.outputs.prs_created == 'true' }}
        env:
          BRANCH: ${{ fromJSON(steps.release.outputs.pr || '{}').headBranchName }}
        run: |
          set -euo pipefail
          uv lock
          if ! git diff --quiet -- uv.lock; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add uv.lock
            git commit -m "chore: sync uv.lock with release version"
            git push origin "HEAD:${BRANCH}"
          fi

      # Enable native auto-merge on the release PR so publishing to PyPI needs
      # no human click. Guarded on the App token being configured: the merge is
      # then attributed to the App (not GITHUB_TOKEN), so the resulting push to
      # main re-triggers this workflow to cut the tag/release — a merge made by
      # GITHUB_TOKEN would not (anti-recursion). Requires branch protection with
      # required status checks on main plus repo "Allow auto-merge"; auto-merge
      # then waits for CI to pass before merging. Without the App the step is
      # skipped and the release PR keeps its current manual-merge flow.
      - name: Enable auto-merge on the release PR
        if: ${{ steps.release.outputs.prs_created == 'true' && vars.RELEASE_BOT_APP_ID != '' }}
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          PR: ${{ fromJSON(steps.release.outputs.pr || '{}').number }}
        run: gh pr merge --auto --squash "${PR}" --repo "${GITHUB_REPOSITORY}"

      # Only needed on the GITHUB_TOKEN fallback path. Tags created with
      # GITHUB_TOKEN do not fire `on: push: tags` (anti-recursion), so we
      # dispatch release.yml manually. When the App token is configured the tag
      # IS created by the App, so `on: push: tags` fires on its own — dispatching
      # here too would publish twice (the second run only survives because
      # release.yml sets skip-existing). Guard on the App being unset to keep
      # exactly one publish per release.
      - name: Trigger PyPI publish workflow
        if: ${{ steps.release.outputs.release_created == 'true' && vars.RELEASE_BOT_APP_ID == '' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ steps.release.outputs.tag_name }}
        # Dispatch on the tag ref: the pypi environment's deployment policy
        # only allows deployments from v* tags, not branches.
        run: gh workflow run release.yml --repo "${GITHUB_REPOSITORY}" --ref "refs/tags/${TAG}" -f tag="${TAG}"
